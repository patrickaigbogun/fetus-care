{
  "name": "fetus-care-fork",
  "version": "0.9.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter ./nextjs dev\" \"cd ./api && poetry run uvicorn main:app --reload\" \"cd golang && air -c .air.toml\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.0"
  }
}
